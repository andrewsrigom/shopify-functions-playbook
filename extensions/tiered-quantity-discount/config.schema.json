{
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean"
    },
    "tiers": {
      "type": "array",
      "minItems": 1,
      "maxItems": 20,
      "items": {
        "type": "object",
        "properties": {
          "minimum": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10000
          },
          "percentage": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100
          }
        },
        "required": ["minimum", "percentage"]
      }
    }
  },
  "required": ["enabled", "tiers"],
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "tiered-quantity-discount configuration",
  "description": "Unknown keys are ignored. Cross-field restrictions are documented in README and enforced by the Function."
}

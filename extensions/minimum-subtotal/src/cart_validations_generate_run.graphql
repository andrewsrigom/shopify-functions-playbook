query CartValidationsGenerateRunInput {
  cart {
    lines {
      id
    }
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
  }
  validation {
    config: metafield(namespace: "$app", key: "config") {
      jsonValue
    }
  }
}

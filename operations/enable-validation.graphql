mutation EnableValidation($id: ID!, $enable: Boolean!) {
  validationUpdate(id: $id, validation: { enable: $enable }) {
    validation {
      id
    }
    userErrors {
      field
      message
    }
  }
}
